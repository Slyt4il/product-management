FlowAccount Technical Test

-  **Frontend**: an Angular app located in the `app/` folder.

-  **Backend**: a .NET Web API located in the `api/` folder.

> The frontend and backend are separate projects and are not integrated.

 The Swagger UI is available at:

`https://localhost:7057/swagger`